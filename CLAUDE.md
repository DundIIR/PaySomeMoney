# PaySomeMoney

Вся документация проекта находится в папке `docs/`.

Перед началом работы прочитай:
- `docs/architecture/overview.md` — общая картина
- `docs/journal/` — последний файл по дате, чтобы понять контекст

После каждой сессии дописывай в журнал что сделано, какие проблемы встретились и что осталось.