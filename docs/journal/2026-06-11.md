# 2026-06-11

## Контекст

Первый файл журнала. Проект PaySomeMoney — сайт с одной кнопкой оплаты 55 ₽ через ЮKassa, статистика видна оплатившим 24 часа. Описание архитектуры: [docs/architecture/overview.md](../architecture/overview.md).

## Сделано (включая прошлые сессии)

- Создан Supabase проект (eu-central-1)
- Создана таблица `payments` с RLS через миграцию
- Добавлены секреты `YOOKASSA_SHOP_ID` и `YOOKASSA_SECRET_KEY`
- Создан каркас фронта на Vite + React + TypeScript
- Установлены `@supabase/supabase-js` и `recharts`
- Создан `.env.local` с `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY`
- Создан `.mcp.json` с MCP сервером Supabase
- Создана структура документации `docs/` (architecture, journal, errors, decisions)

## Проблемы

Не встретилось.

## Следующие шаги

1. Edge Function `create-payment`
2. Edge Function `payment-webhook`
3. Edge Function `get-stats`
4. Фронт: главная страница с кнопкой
5. Фронт: страница возврата с поллингом
6. Фронт: страница статистики с графиком
7. Деплой на Timeweb VPS
8. Мониторинг: Яндекс Метрика + UptimeRobot
